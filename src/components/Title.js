import React, { Component } from 'react';

class Title extends Component {
    render() {
        return(
            <div className="page-header">
                <h1>Project Clone FB <small>React Redux Router API Laravel</small></h1>
            </div>
        );
    }
}
export default Title;